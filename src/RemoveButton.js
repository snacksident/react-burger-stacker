export default function RemoveButton (props){
        return(
            <button 
                onClick={props.clearBurger}
            >
                ditch this burg.
            </button>
        )
}